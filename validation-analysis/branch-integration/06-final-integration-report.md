# Final Integration Report

## Executive Result

`Core Golden Path Live-Validated + Integrated Investigation Depth`, with scope qualification. The result combines main's closed Runtime/Evidence contracts with donor investigation depth without merging donor history or restoring donor-era semantics.

## Branch baseline

- starting main: `4ecf1a9008a53ad464b359db49696ea879161f94`
- starting donor: `95a82258cf56fcf527eaee32391565c3ba6d303e`
- merge base: `61f258829735ac23cfd8be8aad7522bb530e4021`
- verified integration head before closure reports: `163dabbc5b238ac876ab65e29886543f737aff4a`

Semantic migration was used because both branches diverged from the same base: donor breadth contained a five-request Core regression, Stable overclaims, pre-closure validation/lock ordering, and incomplete export handling. Every donor-only file and changed file was reviewed and classified; no donor merge/cherry-pick was used.

## Capabilities

Advanced-only source recipes: performance error/throughput series; alarm events/detail/metric series; recent request response/error/throughput rankings; application instances; external calls; trace exceptions. Each is one fixed serial READ recipe and one immutable SOURCE Run. Core response performance is superseded by main's existing Collect step.

Local-only capabilities: promotion matrix, trace candidate construction/selection, window narrowing/peak location, path triage, error clustering, window/instance/call-tree comparison, external candidate analysis, correction overlays, request budget/reuse planning, and five deterministic workflow plans.

Research-only: uneven component operations and ambiguous `overview.max`. Rejected: all alarm mutation/write operations and donor tests that required five-request Core Collect. Main supersedes donor URL proof, default Collect expansion, stale product status, and pre-closure runtime behavior.

## Core and safety result

Core Collect remains topology + response performance + request-overview candidates: three expected logical requests. Advanced source does not auto-chain, auto-page, or fan out. Core and source use one HTTP executor, Run store, auth/retry/pacing contract, and source resolver. The Core safety allowlist remains separate from the exact Advanced Source READ allowlist; no WRITE, UNKNOWN, research-only, or orphan endpoint is executable.

All 35 main closure contracts are CLOSED in `03-main-contract-preservation-matrix.md`. Source ranking actions retain main's exact actionType resolver and Trace/Navigation proof separation; only responseList's scoped proof may expose `investigate_trace`. External sanitized export now also closes snake_case source identities and dependency URIs.

## Verification and zero-Live result

The final integration suite recorded 106 passing tests, protocol checker PASS, compileall PASS, diff check PASS, local/source/export smoke PASS, exact safety PASS, credential scan PASS, and complete donor unique-file accounting. Private/live root file count and fingerprint remained 5 and `fce901b0f1575b4b43d43df4d140f035625f6e79`; therefore the integration created 0 Live requests/Runs/Raw records and committed no private evidence.

## Git result

Main was fast-forwarded, retested, and pushed. Local and remote `local-before-sync-20260708` were deleted only after `origin/main == main` was confirmed. The task-created integration worktree and branch were removed. Final branch verification showed only local `main` and `origin/main` among the involved branches.

## Final project status

`Core Golden Path Live-Validated + Integrated Investigation Depth`. This does not mean all source capabilities are Live-Proven or every APM domain is production-ready.
